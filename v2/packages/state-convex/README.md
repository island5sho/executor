# persistence-convex

Convex-backed persistence adapter scaffold for Executor v2.

Current scaffold includes:
- Convex table-typed ID schemas in `src/convex-ids.ts`
- shared Convex schema projection in `src/schema.ts`
- first adapter implementation for `ProfileStore` in `src/profiles.ts`
